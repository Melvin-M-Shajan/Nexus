# Optional: local-only convenience so you don't have to paste your key in Settings.
# Copy this file to `.env.local` (gitignored) and add your own key.
# Leave unset for production — each user supplies their own key in Settings.
VITE_GEMINI_API_KEY=
